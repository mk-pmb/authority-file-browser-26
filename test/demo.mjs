// -*- coding: utf-8, tab-width: 2 -*-

const app = {
};
window.afb26demo = app;


async function init() {
  await Promise.all([
  ]);
  console.debug('AFB26 demo init completed.', app);
};





setTimeout(init, 1);

export default app;
